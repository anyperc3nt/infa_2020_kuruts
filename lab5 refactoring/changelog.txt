добавил глобальные переменные цветов, чтобы можно было менять палитру картинки
в функции drawiglu

вместо спама вызовов одной и той же функции реализован цикл по списку данных
Вместо постоянного копипаста *size и ycor xcor реализовано рисование на серфейсе и трансформирование серфейса
Вместо замазки полукруга rect сделано surface с прозрачной заливкой, чтобы можно было рисовать иглу на любых фонах, не только серых


в функции drawescimos

вместо if'a на переменную orientation использованы формулы, что позволило сократить кол-во кода в два раза

